package upei.webserver;

import java.io.PrintStream;

import org.simpleframework.http.Request;
import org.simpleframework.http.Response;
import org.simpleframework.http.core.Container;

public class RouteConfig  implements Container {

	@Override
	public void handle(Request request, Response response) {
		String urlPath = request.getPath().toString();
		
		makeUrlRouter(urlPath);
	}
	
	private void makeUrlRouter(String urlPath, Response response) {
		switch(urlPath) {
			case "api/sprint":
				
				PrintStream body = response.getPrintStream();

				response.setValue("Content-Type", "text/plain");
				body.println("Olá, você requisitou: "+request.getPath());
				body.close();
				break;
		}
	}

}
